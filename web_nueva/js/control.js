/** control.js: Funciones de inicio y control general. */
function Control() {}

/** Código ejecutado en el inicio de la página (onload del body). */
$(document).ready(
  function () { menu.dibujar(); }
);


/** Instancia del objeto. */
var control = new Control();